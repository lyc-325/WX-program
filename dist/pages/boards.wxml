
<view scroll-x="true" style="width:100%" class="navbar _0b2850e">
  <view  wx:for-items="{{navs}}" wx:key="index" data-index="{{index}}" class="item {{currentNav == index ? 'active' : ''}} _0b2850e">
    {{item}}
  </view>
</view>
<view class="list _0b2850e">
  <view  wx:for="{{list}}" bindtap="handleItemTap" wx:key="index" class="item _0b2850e">
    <view class="cover _0b2850e">
      <image mode="scaleToFill" src="{{item.cover}}" class="_0b2850e"/>
    </view>
    <view class="info _0b2850e">
      <view class="title _0b2850e">{{item.title}}</view>
      <view class="content _0b2850e">{{item.content}}</view>
      <view class="footer _0b2850e">
        <view class="createdBy _0b2850e">{{item.createdBy}}</view>
        <view class="createdAt _0b2850e">{{item.createdAt}}</view>
        <view class="read _0b2850e">
          {{item.read}} 阅读
        </view>
      </view>
    </view>
  </view>
</view>
<view bindtap="handlePublishTap" class="publish _0b2850e">发布</view>