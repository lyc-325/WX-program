
<view scroll-x="true" style="width:100%" class="navbar _b924dfd">
  <view  wx:for-items="{{navs}}" wx:key="index" data-index="{{index}}" class="item {{currentNav == index ? 'active' : ''}} _b924dfd">
    {{item}}
  </view>
</view>
<view class="list _b924dfd">
  <view  wx:for="{{list}}" bindtap="handleItemTap" wx:key="index" class="item _b924dfd">
    <view class="cover _b924dfd">
      <image mode="scaleToFill" src="{{item.cover}}" class="_b924dfd"/>
    </view>
    <view class="info _b924dfd">
      <view class="title _b924dfd">{{item.title}}</view>
      <view class="content _b924dfd">{{item.content}}</view>
      <view class="footer _b924dfd">
        <view class="createdBy _b924dfd">{{item.createdBy}}</view>
        <view class="createdAt _b924dfd">{{item.createdAt}}</view>
        <view class="read _b924dfd">
          {{item.read}} 阅读
        </view>
      </view>
    </view>
  </view>
</view>
<view bindtap="handlePublishTap" class="publish _b924dfd">发布</view>